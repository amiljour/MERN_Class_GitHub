import Header from "../components/Header";

const Home = (props) => {
    return (
        <fieldset>
            <legend>Home.jsx</legend>
            🏡🏡🏡🏡
            <Header />
        </fieldset>
    );
};

export default Home;