import React from 'react';

const Header = (props) => {
    return (
        <fieldset>
            <legend>Header.jsx</legend>
            some links here....
        </fieldset>
    );
};

export default Header;